const http = require('http');
const app = require('../app');

const port = parseInt(process.env.PORT, 10) || 7000;
app.set('port', port);
console.log('Server started at port https//localhost:7000')
const server = http.createServer(app);
server.listen(port);
